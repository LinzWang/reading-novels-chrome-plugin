{
  "name": "edge-reading-mode-tts",
  "version": "0.1.0",
  "description": "Microsoft Edge 阅读模式朗读扩展，支持点击切换朗读位置和自动预加载下一页。",
  "type": "module",
  "scripts": {
    "dev": "vite build --config vite.background.config.ts --watch & vite build --config vite.content.config.ts --watch",
    "build": "vite build --config vite.background.config.ts && vite build --config vite.content.config.ts && node scripts/copy-manifest.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@mozilla/readability": "^0.5.0",
    "dompurify": "^3.2.6"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/dompurify": "^3.0.5",
    "@types/node": "^22.10.2",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.3",
    "vitest": "^2.1.8"
  }
}
